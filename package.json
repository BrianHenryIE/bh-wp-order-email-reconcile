{
  "name": "bh-wp-order-email-reconcile",
  "private": true,
  "description": "Development tooling (wp-env + Playwright) for bh-wp-order-email-reconcile.",
  "scripts": {
    "wp-env": "wp-env",
    "wp-env:start": "wp-env start --xdebug",
    "wp-env:stop": "wp-env stop",
    "test:e2e": "tsx node_modules/.bin/playwright test",
    "test:e2e:ui": "tsx node_modules/.bin/playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "*",
    "@wordpress/e2e-test-utils-playwright": "*",
    "@wordpress/env": "*",
    "dotenv": "*",
    "tsx": "*"
  }
}
